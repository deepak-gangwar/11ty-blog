---
title: Homepage
layout: layouts/base.njk
btnTitle: Read the articles
btnHref: /archives
---


{# Hero section #}

{# <section class="hero is-medium is-primary is-bold">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">Hi, I'm {{ site.title }}</h1>
      <h2 class="subtitle">Designer, Writer and Frontend Freelance Developer</h2>
      <h2 class="subtitle">{{ site.description }}</h2>
    </div>
  </div>
</section> #}

<header class="hero">
  <div class="hero__heading">
      <h1 class="hero__title">Deepak</h1>
      <h1 class="hero__title">Gangwar's blog</h1>
      {# <span class="accent contact__accent"></span> #}
  </div>
  <span class="hero__subtitle">Mumblings of a designer x frontend developer</span>
  <br>
  {% include "components/uns-btn.njk" %}
</header>




{# Recent posts section #}

{# 
<section>
  <h1>Posts from sanity</h1>
  {% for post in posts %}
  <h5>{{ post.title }}</h5>
  {% endfor %}
</section> #}

<section class="postlist mt-3 pt-3">
  <div class="container">
    <h2 class="title has-text-centered mt-3 mb-6">Recent posts</h2>
    <div class="columns">
      
      {# The for loop construct in the above snippet is from the Nunjucks 
         templating language, and its one of the ways to iterate over a 
         collection in Eleventy. #}
         
      {% for post in collections.post | reverse %}
        {% if loop.index0 < 3 %}
          <div class="column">
            <div class="card">
              <header class="card-header">
                <p class="card-header-title">
                  {{ post.data.title }}
                </p>
              </header>
              <div class="card-content">
                <div class="content">
                  {{ post.data.description }}
                </div>
              </div>
              <footer class="card-footer">
                <a href="{{ post.url }}" class="button is-fullwidth is-link card-footer-item">Read article</a>
              </footer>
            </div>
          </div>
          {% endif %}
      {% endfor %}
    </div>
  </div>
</section>
