---
title: Post Archives
layout: layouts/base.njk
pagination:
  data: collections.post
  size: 3
  alias: posts
  reverse: true
---

{# Setting variables to be used later #}

{% set allPosts = collections.post | reverse %}
{% set currentPage = 1 %}
{% set postsPerPage = 3 %}



<h2>All articles</h2>
<p>
    This page shares my best articles to read on topics 
    like health, happiness, creativity, productivity and 
    obviously web development. The central question that 
    drives my work is, “How can we live better?” To answer 
    that question, I like to write about science-based ways 
    to solve practical problems.
</p>

{# HOW POSTS WILL LOOK IN ARCHIVES PAGE #}
<ul>
    {% for post in posts %}
        {# Another look for all posts #}
        {# <li><a href="{{ post.url }}">{{ post.data.title }}</a> <i>(written {{ post.date }})</i></li> #}

        {# How list item looks on homepage #}
        <li>
            <div class="card">
                <header class="card-header">
                <p class="card-header-title">
                    {{ post.data.title }}
                </p>
                </header>
                <div class="card-content">
                <div class="content">
                    {{ post.data.description }}
                </div>
                </div>
                <footer class="card-footer">
                <a href="{{ post.url }}" class="button is-fullwidth is-link card-footer-item">Read article</a>
                </footer>
            </div>
        </li>
    {% endfor %}
</ul>


<br>
<br>
<br>


{# --------------   PAGINATION COMPONENT FOR ARCHIVES   --------------- #}

<nav role="navigation" aria-labelledby="pagination">
  <h2 id="my-pagination">This is my Pagination</h2>
  <ul>
    {# --------- First button ----------- #}
    <li>
        {% if page.url != pagination.href.first %}
            <a href="{{ pagination.href.first }}" aria-label="First page">First</a>
        {% else %}
            First
        {% endif %}
    </li>


    {# --------- Previous button -------- #}
    <li>
        {% if pagination.href.previous %}
            <a href="{{ pagination.href.previous }}" rel="prev" aria-label="Previous page">Previous</a>
        {% else %}
            Previous
        {% endif %}
    </li>


    {# --------- Expanded pagination -------------- #}
    {% if pagination.pages.length < 6 %}
        {%- for pageEntry in pagination.pages %}
            <li><a href="{{ pagination.hrefs[ loop.index0 ] }}" aria-label="Page {{ loop.index }}" {% if page.url == pagination.hrefs[ loop.index0 ] %} aria-current="page"{% endif %}>{{ loop.index }}</a></li>
        {%- endfor %}
    {% endif %}


    {# ---------- Breadcrumbed pagination --------- #}
    {% if pagination.pages.length > 6 %}
        {%- for pageEntry in pagination.pages %}
            {% if loop.index0 < 3 %}
                <li><a href="{{ pagination.hrefs[ loop.index0 ] }}" aria-label="Page {{ loop.index }}" {% if page.url == pagination.hrefs[ loop.index0 ] %} aria-current="page"{% endif %}>{{ loop.index }}</a></li>
            {% endif %}
        {%- endfor %}
            <li>...</li>
        {# Now the second for loop is to show two last pages by number #}
        {%- for pageEntry in pagination.pages %}
            {% if loop.index0 > pagination.pages.length-3 %}
                <li><a href="{{ pagination.hrefs[ loop.index0 ] }}" aria-label="Page {{ loop.index }}" {% if page.url == pagination.hrefs[ loop.index0 ] %} aria-current="page"{% endif %}>{{ loop.index }}</a></li>
            {% endif %}
        {%- endfor %}
    {% endif %}


    {# ------------ Next button ----------- #}
    <li>
        {% if pagination.href.next %}
            <a href="{{ pagination.href.next }}" rel="next"  aria-label="Next page">Next</a>
        {% else %}
            Next
        {% endif %}
    </li>


    {# -------- Last button ------------ #}
    <li>
        {% if page.url != pagination.href.last %}
            <a href="{{ pagination.href.last }}" aria-label="Last page">Last</a>
        {% else %}
            Last
        {% endif %}
    </li>
  </ul>
</nav>
